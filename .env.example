OPENROUTER_API_KEY=sk-or-v1-paste_your_key_here
ALLOWED_ORIGINS=http://localhost:5173
VITE_API_URL=http://localhost:8000

# Optional: model used to answer. Defaults to google/gemma-4-26b-a4b-it:free
# OPENROUTER_MODEL=google/gemma-4-26b-a4b-it:free

# Optional: store chat history in PostgreSQL so it survives a restart.
# Without this, history is kept in memory only.
# DAY3_DB_URL=postgresql+psycopg://user:password@localhost:5432/smartlearn